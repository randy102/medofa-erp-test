import SaleOrderMock from '../../support/mock/sale_order_mock'

const mock = new SaleOrderMock()

describe('Create Sale Order', function () {
  // before(() =>{
  //   mock.with_cy(mock.generate)
  // })
  //
  // it('should test', function () {
  //   mock.with_cy(mock._get, ['name']).then(res => {
  //     console.log({res})
  //     cy.wrap(1).should('equal',1)
  //   })
  //   cy.wrap(true).should('equal',true)
  // });
  //
  // after(() => {
  //   mock.cleanup()
  // })
});